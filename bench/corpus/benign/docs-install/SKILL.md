---
name: docs-install
description: Use when installing dependencies for the project.
license: MIT
---
# Dependencies

Install the Python dependencies:

    pip install requests

Install the JavaScript dependencies:

    npm install express

On macOS use Homebrew:

    brew install jq

These instructions are for the user to run in their own terminal.
The skill itself never installs anything.
